# This folder is a placeholder for screenshot assets.
# When the screenshot pass runs, files in this folder will be
# referenced by the handbook pages via relative paths like
# `assets/images/welcome/get-started-localhost.png`.
